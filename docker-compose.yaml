# # docker-compose.yml
# version: '3.8'

# services:
#   ctf-challenge:
#     build:
#       context: .
#       dockerfile: src/docker/Dockerfile
#     #container_name: ctf_netcat_challenge
#     ports:
#       - "4444:4444"       # Challenge port
#     networks:
#       - CTF    #--------------SEE THIS---------------#
#     # Hard resource limits per instance
#     mem_limit: 128m
#     cpus: '0.5'
#     # Security: drop all capabilities except what's needed
#     cap_drop:
#       - ALL
#     cap_add:
#       - NET_BIND_SERVICE
#     # Prevent container breakout
#     security_opt:
#       - no-new-privileges:true
#     # Read-only filesystem except /tmp
#     read_only: true
#     tmpfs:
#       - /tmp:size=10m
#     restart: unless-stopped

#   # Web platform (optional)
#   ctf-web:
#     image: nginx:alpine
#     ports:
#       - "80:80"
#     volumes:
#       - ./web:/usr/share/nginx/html
#     networks:
#       - CTF

# networks:
#   CTF:
#     name: CTF
#     driver: bridge
#     internal: false   # Set true to fully isolate from internet


version: '3.8'

services:
  ctf-challenge:
    build:
      context: .
      dockerfile: src/docker/Dockerfile
    ports:
      - "4444:4444"
    networks:
      - CTF
    mem_limit: 128m
    cpus: '0.5'
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    # removed: no-new-privileges (breaks privesc)
    # removed: read_only (breaks vim/sudo/python)
    restart: unless-stopped

  ctf-web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./web:/usr/share/nginx/html
    networks:
      - CTF

networks:
  CTF:
    name: CTF
    driver: bridge
    internal: false 